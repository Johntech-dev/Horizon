'use server'

export const Signin = async () => {
    try {
        
    } catch (error) {
      console.error('Error', error);
    }
}

export const signUp = async () => {
    try {
        
    } catch (error) {
      console.error('Error', error);
    }
}